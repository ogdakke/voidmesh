# Renderer UI

## Terminology

- **Fixed**: pinned to the viewport/container. It stays in the same screen position while the camera moves.
- **Static**: pinned to a world/canvas coordinate. It stays in the same canvas position while the camera moves.

Do not use `fixed` and `static` interchangeably.
